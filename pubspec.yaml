name: flutter_semver
description: A command-line tool which simplifies the task of updating your Flutter app's version
version: 0.0.1
homepage: https://github.com/RickPoleshuck/flutter_semver

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  pub_semver: ^2.1.0
  yaml: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
